package db;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.util.*;
import beans.PokeBean;
import dao.PokeDao;


/**
 * Servlet implementation class Srv24_PokeUpd1
 */
@WebServlet("/Srv24_PokeUpd1")
public class Srv24_PokeUpd1 extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    /**
     * @see HttpServlet#HttpServlet()
     */
    public Srv24_PokeUpd1() {
        super();
        // TODO Auto-generated constructor stub
    }

	/**
	 * @see HttpServlet#doPost(HttpServletRequest request, HttpServletResponse response)
	 */
	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	try(PokeDao pd = new PokeDao()){	
		PokeBean pb =new PokeBean();
		int pid = Integer.parseInt(request.getParameter("pid"));
		pb.setPid(pid);
		pb.setPname(request.getParameter("pname"));
		pb.setPimg(request.getParameter("pimg"));
		int ptid = Integer.parseInt(request.getParameter("ptid"));
		pb.setPtid(ptid);
//		送信データを基にupdate()を実行
		pd.update(pb);
		
//		更新後の一覧データを取得し、Srv２４_PokeList.jspに転送
		List<PokeBean> list = pd.select();
		request.setAttribute("li", list);
	}catch(Exception e){
		throw new ServletException(e);
	}
	String url="Srv24_PokeList.jsp";
	request.getRequestDispatcher(url).forward(request, response);
	}


}
